// import words from 'an-array-of-english-words';

// // Create a Set for faster lookups
// const dictionary = new Set(words.map(word => word.toLowerCase()));

// export function isValidWord(word) {
//   // Ensure the word is at least 3 letters long and exists in the dictionary
//   return word.length >= 3 && dictionary.has(word.toLowerCase());
// }

// export function validateWords(wordList) {
//   return wordList.filter(word => isValidWord(word));
// }
